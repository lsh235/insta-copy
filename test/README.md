# Test Mode 사용 가이드

이 프로젝트는 디버깅과 테스트를 위한 별도의 test 모드를 제공합니다.

## 📁 구조

```
/test
  ├── test.db          # Test 전용 데이터베이스
  └── seed.ts          # Test 데이터 시드 스크립트

/.env                  # Development 환경 변수
/.env.test            # Test 환경 변수
```

## 🚀 Test 모드 실행

### 1. Test 모드로 개발 서버 시작

```bash
npm run dev:test
```

이 명령어는 `.env.test` 파일의 환경 변수를 사용하여 `test/test.db` 데이터베이스에 연결합니다.

### 2. Test 데이터베이스 초기화

데이터베이스 스키마를 생성하거나 업데이트:

```bash
npm run db:push:test
```

### 3. Test 데이터 시드

테스트용 사용자와 포스트 생성:

```bash
npm run db:seed:test
```

### 4. Test 데이터베이스 확인 (Prisma Studio)

```bash
npm run db:studio:test
```

## 👤 Test 계정

Test 모드에서는 다음 계정들을 사용할 수 있습니다:

| Email | Username | Password |
|-------|----------|----------|
| test1@test.com | test_user_1 | test123 |
| test2@test.com | test_user_2 | test123 |
| test3@test.com | test_user_3 | test123 |

## 📝 포스트 데이터

- 총 6개의 테스트 포스트
- 각 포스트는 `[TEST]` 태그로 표시됨
- 좋아요, 댓글, 팔로우 관계 포함

## 🔄 Development 모드로 전환

일반 개발 모드로 돌아가려면:

```bash
npm run dev
```

이 명령어는 `.env` 파일의 환경 변수를 사용하여 `prisma/dev.db` 데이터베이스에 연결합니다.

## 💡 사용 시나리오

### 새로운 기능 테스트
```bash
# Test 환경 설정
npm run db:push:test
npm run db:seed:test

# Test 모드로 서버 실행
npm run dev:test
```

### 데이터베이스 리셋
```bash
# Test DB 삭제
rm test/test.db

# 다시 생성
npm run db:push:test
npm run db:seed:test
```

## 🎯 장점

1. **격리된 환경**: 개발 데이터베이스에 영향 없이 테스트 가능
2. **빠른 리셋**: 테스트 데이터를 쉽게 초기화
3. **재현 가능**: 항상 동일한 테스트 데이터로 시작
4. **디버깅 용이**: 예측 가능한 데이터로 버그 재현

## 📚 관련 스크립트

```json
{
  "dev:test": "Test 모드로 개발 서버 실행",
  "db:push:test": "Test DB 스키마 생성/업데이트",
  "db:seed:test": "Test DB에 샘플 데이터 추가",
  "db:studio:test": "Test DB를 Prisma Studio로 열기"
}
```
