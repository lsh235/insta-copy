# Database
DATABASE_URL="file:./dev.db"

# NextAuth
# Generate a secret with: openssl rand -base64 32
AUTH_SECRET="your-secret-here"

# NextAuth URL (set to your app URL, e.g., http://localhost:3000 for dev)
AUTH_URL="http://localhost:3000"

# Set to "true" to trust host headers (needed for proxied environments like GitHub Codespaces)
# Only enable in development or when behind a trusted proxy
AUTH_TRUST_HOST="true"
